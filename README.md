# Real-time-character-counter-
Lightweight web app that tracks text input with a strict 50-char limit. Built with vanilla HTML/CSS/JS, no dependencies. Counts in real time, auto-trims pasted text, turns red at 50/50. Great for forms, bios, tweets. Runs client-side. Open index.html to use. MIT licensed.
